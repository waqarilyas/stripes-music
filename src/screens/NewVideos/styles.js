import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    color: 'white',
    alignSelf: 'center',
    fontWeight: 'bold',
    fontSize: 22,
    marginVertical: 20,
  },
});

export default styles;
